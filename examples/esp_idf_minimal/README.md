# esp_idf_minimal

Minimal `app_main()` example for this component.

## Use

1. Add this component to your ESP-IDF project (`components/fastled` or component manager).
2. Copy `main.cpp` contents into your project's `main/` component.
3. Update:
   - `kDataPin` for your wiring
   - `kNumLeds` for your strip length
